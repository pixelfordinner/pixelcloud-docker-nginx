server {
    listen                  80 deferred;
    listen                  [::]:80 deferred;
    server_name             nginx.local.dev;
    root                    /opt/www/htdocs;

    # Enable Security
    include                 boilerplate/enable/security.conf;

}
